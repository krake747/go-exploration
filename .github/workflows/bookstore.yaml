name: "bookstore"

on:
  push:
    paths:
      - "src/bookstore/**"
      - ".github/workflows/bookstore.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Run tests
        run: |-
          cd src/bookstore
          go test ./...
