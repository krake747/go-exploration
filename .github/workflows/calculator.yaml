name: "calculator"

on:
  push:
    paths:
      - "src/calculator/**"
      - ".github/workflows/calculator.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Run tests
        run: |-
          cd src/calculator
          go test ./...
